@inherits LayoutComponentBase

@*
    Kiosk display layout — intentionally chromeless so the slideshow looks
    like only a slideshow. No top bar, no sidebar, no margins. Pages opt in
    by adding `@layout DisplayLayout`.
*@

@Body
